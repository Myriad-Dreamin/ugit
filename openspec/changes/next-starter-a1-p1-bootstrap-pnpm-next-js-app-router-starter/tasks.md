## 1. Proposal Alignment

- [x] 1.1 Review the approved OpenSpec artifacts for "Bootstrap pnpm Next.js App Router starter" and confirm the canonical request/PR title is `feat: Bootstrap pnpm Next.js App Router starter`
- [x] 1.2 Confirm the proposal is ready for pooled execution, the reusable worktree from `/home/kamiyoru/work/ts/ugit/.meow-team-worktrees/meow-N` can be claimed, and conventional-title metadata `feat` stays separate from `branchPrefix` and change paths

## 2. Implementation

- [x] 2.1 Implement the approved objective: Initialize the empty `ugit` repository as a full-TypeScript Next.js App Router app with a minimal Hello World page, `@/` imports, harness-aligned ESLint/Prettier/Vitest configuration, root `INSTRUCTIONS.md`/`AGENTS.md`/`TODO.md`, required package scripts and dependencies, and passing `pnpm fmt`, `pnpm lint`, `pnpm test`, and `pnpm build`.
- [x] 2.2 Run validation and capture reviewer findings for "Bootstrap pnpm Next.js App Router starter"
