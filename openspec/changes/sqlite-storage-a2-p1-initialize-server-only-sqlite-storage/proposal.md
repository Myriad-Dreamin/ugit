## Why

Create one implementation-ready change that adds a server-only `better-sqlite3` foundation with normalized path resolution, cached shared connections, handwritten migrations and metadata helpers, a small homepage-content storage domain replacing the current static `lib/hello.ts` helper, focused Vitest coverage for `:memory:` and temp-file databases, and full validation through `pnpm fmt`, `pnpm fmt:check`, `pnpm lint`, `pnpm test`, and `pnpm build`. Implement one OpenSpec-aligned change that introduces a reusable server-only SQLite layer and moves the current homepage message off the static helper into a small storage domain, then validate with the existing pnpm toolchain. This proposal is one candidate implementation for the request: You are working in a TypeScript Next.js App Router repository. Task: initialize SQLite storage in the same style as a clean Next.js server-only setup that uses `better-sqlite3`, handwritten migrations, cached connections, WAL mode, and a small storage abstraction layer. Do the implementation, not just a proposal. Requirements 1. First inspect the repo and identify: - where server-only utilities live - current persistence approach, if any - any config file that should own the SQLite file path - existing test runner and formatting/lint commands 2. Implement a server-only SQLite foundation, preferably under something like `lib/storage/sqlite.ts`, with these behaviors: - use `better-sqlite3` - export a `DatabaseSync` type alias from `better-sqlite3` - resolve storage paths so relative paths are rooted at `process.cwd()` - support `:memory:` for tests - if a path ends in `.sqlite`, use it directly - otherwise map `<path>` to `<path>.sqlite` - create the parent directory automatically - configure SQLite with: - `busy_timeout = 5000` - `foreign_keys = ON` - `journal_mode = WAL` for file-backed databases only - cache one shared connection per SQLite path in server-only global state so Next.js does not reopen the same database repeatedly - expose a test-only cache reset helper that closes cached databases 3. Add a small migration system in the shared SQLite module: - bootstrap a `schema_migrations` table if missing - support ordered handwritten migrations with: - `version` - `name` - `up(database)` - run migrations idempotently - apply each migration inside an `IMMEDIATE` transaction - track applied migrations in `schema_migrations` - add a `storage_metadata` table and keep these metadata values synchronized: - `storage_engine=better-sqlite3` - `created_at` - `schema_version` - expose helpers similar to: - resolve storage location - get storage state - with storage - apply migrations - read storage metadata - queue storage mutation - run SQLite transaction - check whether a table exists 4. Create one domain-specific storage module, preferably under something like `lib/storage/<domain>.ts`, that uses the shared SQLite helpers instead of raw ad hoc setup scattered around the app. - choose the domain based on the repo’s actual needs - keep the domain table simple and explicit - store timestamps as ISO strings - use parameterized SQL only 5. Keep this Next.js-safe: - mark storage modules `server-only` - do not import them into client components - keep route handlers or server actions thin; push storage logic into `lib/storage/*` 6. Add tests that verify: - migrations apply in order and are idempotent - metadata is written correctly - the domain table exists after migrations - the connection cache reuses the same database for equivalent paths - `:memory:` works for migration tests - temp `.sqlite` files work for integration-style storage tests 7. Validation: - run the repo’s formatter - run lint - run the relevant tests - if the repo has a build command, run it if feasible Implementation style - prefer small focused modules - use plain SQL, not Prisma/Drizzle/etc., unless the repo already uses one of them and changing that would be inconsistent - keep all non-i18n text in English - avoid client-side SQLite usage - do not add unnecessary abstractions Deliverables - make the code changes - summarize the architecture briefly - list the files changed - report the exact validation commands run and their results - call out any assumptions if the repo’s existing storage model was unclear.

## What Changes

- Introduce the `sqlite-storage-a2-p1-initialize-server-only-sqlite-storage` OpenSpec change for proposal "Initialize server-only SQLite storage".
- Create one implementation-ready change that adds a server-only `better-sqlite3` foundation with normalized path resolution, cached shared connections, handwritten migrations and metadata helpers, a small homepage-content storage domain replacing the current static `lib/hello.ts` helper, focused Vitest coverage for `:memory:` and temp-file databases, and full validation through `pnpm fmt`, `pnpm fmt:check`, `pnpm lint`, `pnpm test`, and `pnpm build`.
- Keep the proposal logically scoped so any approved coding-review worker can claim it without replanning.

## Capabilities

### New Capabilities
- `sqlite-storage-a2-p1-initialize-server-only-sqlite-storage`: Create one implementation-ready change that adds a server-only `better-sqlite3` foundation with normalized path resolution, cached shared connections, handwritten migrations and metadata helpers, a small homepage-content storage domain replacing the current static `lib/hello.ts` helper, focused Vitest coverage for `:memory:` and temp-file databases, and full validation through `pnpm fmt`, `pnpm fmt:check`, `pnpm lint`, `pnpm test`, and `pnpm build`.

### Modified Capabilities
- None.

## Conventional Title

- Canonical request/PR title: `feat(storage/sqlite): Initialize server-only SQLite storage`
- Conventional title metadata: `feat(storage/sqlite)`
- Slash-delimited roadmap/topic scope stays in conventional-title metadata and does not alter `branchPrefix` or OpenSpec change paths.


## Impact

- Affected repository: `ugit`
- Coding-review execution: pooled workers with reusable worktrees from `/home/kamiyoru/work/ts/ugit/.meow-team-worktrees/meow-N`
- Planner deliverable: Suggested OpenSpec change: `sqlite-storage-a2-p1-initialize-server-only-sqlite-storage`.\n\nImplementation objective:\n- Add `better-sqlite3` as a direct runtime dependency and add typings only if the chosen version still needs them.\n- Create a small server-only config owner for the SQLite path, likely `lib/storage/config.ts`, with an env override such as `UGIT_STORAGE_PATH` and a repo-relative default like `data/ugit`. Relative paths must resolve from `process.cwd()`, `:memory:` must remain supported for tests, `.sqlite` paths must pass through unchanged, and other file names must normalize to `<path>.sqlite`.\n- Add `lib/storage/sqlite.ts` with an explicit `server-only` import, a `DatabaseSync` alias re-export, normalized storage-location resolution, parent-directory creation, per-path global connection caching, PRAGMA setup (`busy_timeout = 5000`, `foreign_keys = ON`, WAL only for file-backed DBs), `resolveStorageLocation`, `getStorageState`, `withStorage`, `queueStorageMutation`, transaction helper, table-existence helper, and a test-only cache reset that closes cached databases.\n- Build the shared migration system in that module: bootstrap `schema_migrations` and `storage_metadata` if missing, support ordered handwritten migrations with `version`, `name`, and `up(database)`, run them idempotently, apply each migration inside an `IMMEDIATE` transaction, record applied versions, and keep `storage_engine=better-sqlite3`, `created_at`, and `schema_version` synchronized.\n- Choose the homepage greeting as the first storage domain because the current app has no other persisted domain. Replace the current `lib/hello.ts` flow with a server-only module such as `lib/storage/homepage.ts` that defines the domain migration set, keeps timestamps as ISO strings, uses parameterized SQL only, exposes explicit read and mutation helpers, and lets `app/page.tsx` stay thin.\n- Add Vitest coverage for path normalization, cache reuse across equivalent paths, ordered and idempotent migrations, metadata values, `:memory:` migration runs, domain-table creation, and temp-file integration flows. Keep tests in the existing node-based `*.test.ts` pattern and clean up temp files or directories after integration cases.\n- Keep client boundaries safe: only server components or other server code may import `lib/storage/*`. If Next.js build needs native-module externalization for `better-sqlite3`, add the smallest possible `next.config.ts` with `serverExternalPackages`.\n\nAssumptions and risks:\n- There is no existing persistence model, so homepage content is the only coherent first domain.\n- There is no existing config owner for storage path, so creating one is part of the proposal rather than reusing a non-existent config surface.\n- Native-module bundling may require minimal Next.js config even though the repo currently has none.\n\nValidation contract after implementation:\n- `pnpm fmt`\n- `pnpm fmt:check`\n- `pnpm lint`\n- `pnpm test`\n- `pnpm build`\n\nPool coordination: no coder or reviewer lane should start until human approval arrives.
